version: '3.8'

services:
  repro:
    image: alpine:3.18
    command: sh -c "printenv | tee /tmp/env.txt && sleep 300"
    environment:
      - LLM_API_KEY=${NVIDIA_NIM_API_KEY}
    env_file:
      - .env
    restart: "no"