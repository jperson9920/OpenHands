# .env.example
# Example environment variables for local development (DO NOT commit real secrets)

# NVIDIA NIM API key (format: nvapi-xxxxxxxxxx)
NVIDIA_NIM_API_KEY=nvapi-xxxxxxxxxx

# Optional / historical keys (keep locally if needed)
# VSC_LM_API_KEY=your-vsc-lm-key
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# Windows PowerShell example to create a local .env (run locally, DO NOT commit)
# PowerShell:
#   Set-Content -Path .env -Value "NVIDIA_NIM_API_KEY=nvapi-xxxxxxxxxx"

# Docker usage
# docker-compose will read variables from the host environment or a .env file in this directory.
# Ensure your real NVIDIA_NIM_API_KEY is set in a local .env or in your user environment variables.

# Security reminder: keep real API keys out of version control and never paste them into public logs.